# 存储与目录约定

这个仓库默认把运行时文件分成两类：
- `./data`
  - 持久化数据库和输出数据

## 推荐目录说明

### `./data`

这里会保存：
- SQLite 数据库
- 输出目录
- 其他运行时持久化数据

这个目录应该保留，升级容器时不要删。

## 备份建议

至少备份：
- `./data`

如果你使用增强版，也建议同时确认：
- 当前使用的镜像版本已记录
- 当前绑定参数已记录

## 不建议的做法

- 不要把私钥放进这个仓库
- 不要只记住 `latest`，正式环境一定要记录实际使用的版本标签
